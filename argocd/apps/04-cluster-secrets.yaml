# The ExternalSecret objects — wave -1, before anything that reads the Secrets
# they produce.
#
# External Secrets Operator itself is NOT here: it is part of the Terraform seed,
# because ArgoCD needs a working ESO to fetch the deploy key it reads this
# repository with. Its CRDs therefore already exist by the time this syncs.
#
# Kept as raw manifests rather than a chart because they are three small objects
# specific to this cluster, and wrapping them in a chart would add a layer with
# nothing in it.
#
# SSH URL, not HTTPS: this repository is private and ArgoCD reads it with a
# read-only deploy key.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-secrets
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: default
  source:
    repoURL: git@github.com:mhassaankhokhar/gke-3tier-gitops.git
    targetRevision: main
    path: manifests/external-secrets
  destination:
    server: https://kubernetes.default.svc
    # Namespace here is only the default for objects that do not set their own;
    # each ExternalSecret names its own namespace.
    namespace: external-secrets
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
