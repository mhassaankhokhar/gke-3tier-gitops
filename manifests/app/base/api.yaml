# The api tier: the only thing that talks to Postgres.
#
# Its own ServiceAccount, because identity is what the mesh's authorization
# rules are written against — "allow from the web service account" is a claim
# Envoy can verify with mTLS, unlike a namespace or an IP.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: api
automountServiceAccountToken: false
---
# A Rollout, not a Deployment.
#
# A Deployment can only do a rolling update: it swaps pods a few at a time and
# has no notion of sending a fraction of *traffic* anywhere. A canary is a
# traffic decision, so it needs the mesh to split requests and this controller
# to move the split over time.
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: api
spec:
  replicas: 2
  # Keep the previous version's ReplicaSet so a failed canary can be aborted
  # back to something that is already running, rather than re-pulled.
  revisionHistoryLimit: 2
  strategy:
    canary:
      # Two Services selecting the same pods; Rollouts keeps their selectors
      # pointed at the right ReplicaSet as the canary progresses.
      canaryService: api-canary
      stableService: api-stable
      trafficRouting:
        istio:
          virtualService:
            name: api
            # Must match the named http route in the VirtualService — Rollouts
            # rewrites the weights on that route and nothing else.
            routes:
              - primary
      # Percentages, with a pause at each. The pauses are what make this a
      # canary rather than a slower rolling update: they are where you watch
      # error rates before more traffic moves.
      steps:
        - setWeight: 20
        - pause: { duration: 60s }
        - setWeight: 50
        - pause: { duration: 60s }
        - setWeight: 100
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      serviceAccountName: api
      # Stateless: it holds nothing a preemption would lose, so it belongs on
      # the cheap pool. That is the whole reason the pools are split.
      nodeSelector:
        workload: stateless
      containers:
        - name: api
          image: api
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: PORT
              value: "3000"
            # -rw is CloudNativePG's service for the primary. Writes must not
            # go to -ro or -r, which point at replicas.
            - name: DBHOST
              value: postgres-rw.database.svc.cluster.local
            - name: DBPORT
              value: "5432"
            - name: DB
              value: app
            # Projected from Secret Manager by External Secrets — the same
            # object CloudNativePG bootstrapped the user from, so the two cannot
            # drift. The password is in neither Git nor Terraform state.
            - name: DBUSER
              valueFrom:
                secretKeyRef:
                  name: postgres-app-credentials
                  key: username
            - name: DBPASS
              valueFrom:
                secretKeyRef:
                  name: postgres-app-credentials
                  key: password
            # CloudNativePG serves TLS 1.3 and issues its own certificates, so
            # this connection is encrypted without the mesh being involved.
            - name: DBSSL
              value: "true"
            # Redis moved here from the web tier. It was a session store for a
            # server-rendered page; a SPA talking to a JSON API has no
            # server-side session, so rather than keep a component with no
            # purpose it now caches the summary aggregation — three queries
            # every page load wants. The cache fails open: if Redis is down this
            # gets slower, not broken.
            - name: REDIS_URL
              value: redis://redis.app.svc.cluster.local:6379
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
          # Readiness checks the dependency: a pod that cannot reach it should
          # leave the Service. Failing this removes traffic, nothing more.
          readinessProbe:
            httpGet:
              path: /readyz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
          # Liveness asks only whether the process is alive, and asks it of a
          # handler that touches nothing else.
          #
          # Both probes previously pointed at /api/status — the expensive path,
          # reaching Postgres. Under load it took 2-5s against the 1s default
          # timeout, so Kubernetes killed pods that were healthy and merely
          # busy, and the survivors inherited the load and were killed in turn.
          # 9 and 11 restarts in a single load test.
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10
            # Generous on purpose. A liveness probe exists to catch a wedged
            # process, not a slow one — and killing a busy pod makes the load
            # worse, never better.
            timeoutSeconds: 5
            failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: 3000
---
# Two more Services, both selecting app=api.
#
# Rollouts adds a pod-template-hash to each selector as the canary runs, so at
# any moment `api-stable` points at the old ReplicaSet and `api-canary` at the
# new one. They look redundant in Git and are not at runtime.
apiVersion: v1
kind: Service
metadata:
  name: api-stable
spec:
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: api-canary
spec:
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: 3000
---
# The split itself.
#
# No `gateways` field, so this applies to mesh-internal traffic — which is all
# the api gets, since only web calls it. The weights below are the starting
# point; Argo Rollouts rewrites them on the route named `primary` as it walks
# the steps, and writes them back to 100/0 when the canary is promoted or
# aborted. Git holds the resting state, not the in-flight one.
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: api
spec:
  hosts:
    - api
  http:
    - name: primary
      route:
        - destination:
            host: api-stable
          weight: 100
        - destination:
            host: api-canary
          weight: 0
